@page
@model PortalArcomix.Pages.GestaoProdutoVEModel
@{
    ViewData["Title"] = "GestaoProdutoVE";
    Layout = "_Layout";
}

<div class="container-fluid">
    <div class="row align-items-center" style="margin-top: 50px;">
        <div class="col">
            <div class="card">
                <div class="card-body framelogin">
                    <div class="row">
                        <div class="col text-center">
                            <h3 style="color: #1878BB;">Gestão de Produtos</h3>
                            <hr />
                        </div>
                    </div>

                    <div class="gap-2">
                        <a id="CadastroProdutoBotao" class="btn btn-primary" href="/ProdutoVE" role="button">Cadastrar Novo Produto</a>
                    </div>

                    <hr />

                    <!-- Filter inputs -->
                    <div class="container-fluid">
                        <form method="post">
                            <div class="row">
                                <div class="ms-auto col-md-3">
                                    <input type="text" name="Filter" class="form-control" value="@Model.Filter" placeholder="Filtre campos separando por %" />
                                </div>
                                <div class="col-md-2">
                                    <button type="submit" class="btn btn-success">Filtrar</button>
                                </div>
                            </div>
                        </form>
                    </div>
                    <hr />

                    <!-- Table -->
                    <div class="row">
                        <div class="col">
                            <table class="table table-striped table-bordered">
                                <thead>
                                    <tr>
                                        <th>ID</th>
                                        <th>Marca</th>
                                        <th>Descrição do Produto</th>
                                        <th>Gestor de Compras</th>
                                        <th>Ações</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var produto in Model.Produtos)
                                    {
                                        <tr>
                                            <td>@produto.ID</td>
                                            <td>@produto.MARCA</td>
                                            <td>@produto.DESCRICAOPRODUTO</td>
                                            <td>@produto.GESTORCOMPRAS</td>
                                            <td>
                                                <a href="/ProdutosVE?id=@produto.ID" role="button">
                                                    <i class="fa fa-edit"></i>
                                                </a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                    <hr />
                </div>
            </div>
        </div>
    </div>
</div>
<br />
